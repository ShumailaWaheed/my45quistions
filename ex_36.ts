function make_shirt(size: string, message: string) {
    console.log(`The shirt size is ${size} 
    and it has the following message: "${message}".`);
}

// Call the function
make_shirt("Medium", "I love My Father!");